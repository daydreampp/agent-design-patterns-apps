# Kiro Skill

Kiro does not use the Codex/Claude marketplace format. The installer copies this skill into:

```bash
~/.kiro/skills/agent-design-patterns
```

The copied skill includes the full Agent Design Patterns repository under `repository/`.
